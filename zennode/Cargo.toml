[package]
name = "zennode"
version = "0.1.1"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0 OR MIT"
description = "Self-documenting node definitions for image processing pipelines"
readme = "../README.crates.md"
repository = "https://github.com/imazen/zennode"
keywords = ["image", "pipeline", "node", "schema"]
categories = ["multimedia::images"]
exclude = [
  "CLAUDE.md", "CONTEXT-HANDOFF.md", "FEEDBACK.md",
  ".claude/", ".superwork/", ".zenbench/", "copter-report/",
  ".github/", "Cross.toml", "justfile", "Cargo.toml.original.txt", "fuzz/",
  "tests/",
  "ENCODE-FULL.md", "ENCODE-PATHS.md", "SPEC.md",
]

[features]
default = ["std", "derive"]
std = []
derive = ["dep:zennode-derive"]
serde = ["dep:serde", "dep:serde_json", "std"]
json-schema = ["serde"]

[dependencies]
zennode-derive = { version = "0.1.1", optional = true }
serde = { version = "1.0.229", default-features = false, features = ["derive", "alloc"], optional = true }
serde_json = { version = "1.0.151", optional = true }

[dev-dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
